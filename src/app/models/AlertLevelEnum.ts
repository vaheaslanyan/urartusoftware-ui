/**
 * An enum that represents different styles for alert used to define their appearance, corresponds to Bootstrap styling.
 */
export enum AlertLevelEnum {
    PRIMARY,
    SECONDARY,
    SUCCESS,
    DANGER,
    WARNING,
    INFO,
    LIGHT,
    DARK
}